import type { UserUsageSettings } from '@lobechat/types';

export const DEFAULT_COST_ESTIMATE_WARNING_THRESHOLD = 5;

export const DEFAULT_USAGE_SETTINGS: UserUsageSettings = {
  costEstimateWarningThreshold: DEFAULT_COST_ESTIMATE_WARNING_THRESHOLD,
};
